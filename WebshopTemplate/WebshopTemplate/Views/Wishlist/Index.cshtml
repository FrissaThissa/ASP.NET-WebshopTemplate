@model WebshopTemplate.Models.Wishlist

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Wishlist</h1>

<table class="table">
    <tbody>
@foreach (var item in Model.Products) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-action="RemoveProduct" asp-route-id="@item.Id">X</a>
            </td>
        </tr>
}
    </tbody>
</table>